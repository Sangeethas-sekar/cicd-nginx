name: trigger-jenkins
on:
 push:
  branches: [master]

jobs:
 trgr-jen:
  runs-on: ubuntu-latest
   steps:
    - name: checkout code
      uses: actions/checkout@v3

    - name: jen job
      run: |
       curl -X POST \
        http://3.109.181.54:8080/job/nginx.job/build \
         --user admin:${{ secrets.JENKINS_TOKEN }}

